{
  "name": "api-lambda-jwt",
  "version": "0.1.0",
  "bin": {
    "api-lambda-jwt": "bin/api-lambda-jwt.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.113.0",
    "aws-cdk": "1.113.0",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "@aws-cdk/core": "1.113.0"
  }
}
